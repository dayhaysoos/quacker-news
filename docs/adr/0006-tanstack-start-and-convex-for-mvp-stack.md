# TanStack Start And Convex For The MVP Stack

The MVP uses TanStack Start for the reader-facing app and Convex for product data, run data, backend functions, cron jobs, and scheduled agent work. We chose this over Next.js because the project owner prefers the TanStack ecosystem, and over adding a separate API server because Convex already covers the MVP backend needs.
